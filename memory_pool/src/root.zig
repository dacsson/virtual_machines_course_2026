const benchmark = @import("benchmark.zig");
pub const benchmarkPool = benchmark.benchmarkPool;
pub const benchmarkHeap = benchmark.benchmarkHeap;

pub const PoolpFictionAllocator = @import("pool.zig");
